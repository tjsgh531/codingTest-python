def solution(rows, columns, queries):
    # ((i-1) x columns + j)
    bord = []
    for r in range(rows):
        temp = []
        for c in range(columns):
            temp.append = (r-1) * columns + c
        bord.append(temp)
    
    
    answer = []
    return answer

def rotateBord(list1):
    row1 = list1[0]
    row2 = list1[2]
    col1 = list1[1]
    col2 = list1[3]

    heap? hash??